
def generate_answer(question: str, document: str) -> str:
    """Return the document as the answer. Placeholder for future LLM-based generation."""
    # In production, this could be replaced by a locally hosted language model.
    return document
